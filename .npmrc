# Prevent installation of optional platform-specific binaries during CI (e.g. @rollup/rollup-win32-x64-msvc)
# This makes `npm install` skip optionalDependencies which avoids EBADPLATFORM on Linux builders.
optional=false
